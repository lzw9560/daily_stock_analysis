# Cloudflare Pages 配置
name = "stock-analysis"
compatibility_date = "2024-01-01"

# Pages 构建配置
[build]
command = "npm install && CLOUDFLARE_PAGES=1 npm run build"
output_directory = "dist"

# 环境变量
[vars]
CLOUDFLARE_PAGES = "1"
